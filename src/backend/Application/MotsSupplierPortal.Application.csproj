<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Domain\MotsSupplierPortal.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="FluentValidation" Version="11.11.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- EPIC-15's copy catalogue. Embedded for the same reason §7.2's is: one copy, unmodifiable in
         a deployed environment without a rebuild, loadable without a file path. -->
    <EmbeddedResource Include="Notifications\NotificationCatalogue.jsonc" />
  </ItemGroup>

</Project>
