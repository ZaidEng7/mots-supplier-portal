namespace MotsSupplierPortal.Domain;

// PROBE (throwaway): deliberate compile error to prove branch protection blocks a merge with a
// genuinely red Backend (.NET) check. This file and its branch will be deleted immediately after.
public sealed class ProbeBrokenBuild
{
    public void ThisWontCompile( this is not valid C# syntax at all )
    {
    }
}
